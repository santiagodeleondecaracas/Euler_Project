Problem 5: Smallest multiple
2520 is the smallest number that can be divided by each of the numbers from 1 to 10 without any remainder.

What is the smallest positive number that is evenly divisible by all of the numbers from 1 to n?

Tests

Test 1. smallestMult(5) should return a number.
Test 2. smallestMult(5) should return 60.
Test 3. smallestMult(7) should return 420.
Test 4. smallestMult(10) should return 2520.
Test 5. smallestMult(13) should return 360360.
Test 6. smallestMult(20) should return 232792560.

def smallest_mult(n):
    test_number = 2
    count = 0
    limit = 1000000000
    
    while test_number <= limit:
        for i in range(1, n + 1):
            if test_number % i == 0:
                count += 1
            else:
                break
        
        if n == count:
            print('testNumber:', test_number, 'N:', n, 'Count:', count, "It works out this time")
            return test_number
        
        count = 0
        test_number += 2



print(smallest_mult(5))
